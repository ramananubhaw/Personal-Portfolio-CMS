export type Navs = {
    about: boolean,
    skills: boolean,
    experience: boolean,
    projects: boolean
};

export type AccountType = {
    platform: string,
    link: string
}

export type PersonalInfo = {
    name: string,
    email: string,
    phone: string,
    resumeLink: string
}